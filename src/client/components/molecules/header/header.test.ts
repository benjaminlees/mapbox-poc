import { updateVal } from './header';

// this is a placeholder test and should be removed
test('Header component: updateVal should run the modulo operator with the divisor 3', () => {
  expect(updateVal(3)).toBe(0);
});
